import { useEffect } from "react";

export const Contact = ({ }) => {
    /* Esto se usa para cambiar el titulo de la Ruta */
    /* useEffect(()=>{
        document.title="Contacto!";
    },[]); */
    return (
        <div>
            Contacto
        </div>
    );
};